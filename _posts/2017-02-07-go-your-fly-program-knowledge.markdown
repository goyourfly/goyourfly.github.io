---
layout: post
title: "算法学习中的一些小知识"
date: 2017-02-07
---

## 算法性能

* 算法性能分级：常数级、对数级、次线性级、nlog(n)级、平方级、指数级
  * 基础操作通常被认为是常数级O(1) (如 2 > 1)
  * 对数级算法一般是指每次将问题规模缩减一半，对数级算法性能高效
* log(n) = x 则 2^x = n:在算法中，表示算法对数级性能时，通常是log(n)，其中log的未标明的底数默认是2
